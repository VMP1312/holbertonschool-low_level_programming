#include <stdio.h>
#include <string.h>
/**
 * main - Print a string.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char string[80] = "\"Programming is like building a multilingual puzzle";

	puts(string);
	return (0);
}
