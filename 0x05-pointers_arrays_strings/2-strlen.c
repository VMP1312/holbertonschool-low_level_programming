#include "holberton.h"
/**
 * _strlen - return the length of a string
 * @s: pointer of the array
 * Return: length
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
	{
	}
	return (c);
}
